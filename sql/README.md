# SQL

This directory is reserved for durable database-level work:

- migrations and schema changes;
- reusable views and materialized views;
- roles and permissions;
- repeatable administration or setup SQL.

The first schema currently remains visible in `notebooks/01_build_database_model.ipynb`. Exploratory queries are not duplicated here without a reuse or deployment reason.
