# Copy this file to .env and replace the placeholder values.
# MAna.database.postgres_url_from_env() reads these standard PG variables.
# Never commit .env.
PGHOST=localhost
PGPORT=5432
PGDATABASE=mansoura_mobility
PGUSER=postgres
PGPASSWORD=change_me
PGSSLMODE=prefer
